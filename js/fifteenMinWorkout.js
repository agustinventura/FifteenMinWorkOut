
function init() {
	setBackKeyListener();
	//tizen.humanactivitymonitor.start('HRM', hrmListener);
	//tizen.power.request("SCREEN", "SCREEN_NORMAL");
	//showWelcomeScreen();
}

$(document).ready(init);